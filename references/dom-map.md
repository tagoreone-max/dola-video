# dola 选择器校准记录（dom-map）

> 跑通生成流程后，把实测选择器填到这里，之后脚本直接复用，不再探测。
> 状态含义：✅ 实测 = 已在真实页面命中；⏳ 待校准 = 逻辑可用但未逐项验证。

## 实测记录

| 元素 | 选择器/文本 | 状态 |
|---|---|---|
| 输入框 | `div.tiptap.ProseMirror` | ✅ 实测 |
| 视频生成入口 | 底部工具栏文本「视频生成」 | ✅ 实测 |
| 上传控件 | 视频生成模式下的常驻 `input[type=file]`（accept 含 .jpg/.png/.jpeg/.webp），直注 `set_input_files` | ✅ 实测（多图追加） |
| 比例按钮 | 中文文案，值 9:16 / 16:9 / 3:4 / 4:3 / 1:1 / 21:9 / 自动 | ✅ 实测（展开为点击当前值再选项） |
| 时长档位 | 4s / 5s / 10s / 15s | ⏳ 待校准（5s 为兜底猜测，用 `generate.py --list` 核实） |
| 模型 | Seedance 系 | ⏳ 待校准（具体档位文案） |
| 发送按钮 | **不存在**——填完提示词直接 `Enter` 提交 | ✅ 实测（generate.py 已实现 Enter fallback） |
| 状态文案 | 「生成中」/「排队」/「创作中」 | ✅ 实测 |
| 视频元素 | `video` 标签，取 `src`/`currentSrc`（同豆包轮询逻辑） | ✅ 实测 |
| 下载 | 页面内 fetch 被隐私框架拦 → **CDN 直链回退** | ✅ 实测（回退分支成功） |
| 水印 | 右下角静态烧录，单帧字符画定位 | ✅ 实测（坐标每次复核，见 pitfalls §0.7） |

## 校准方法

```bash
python scripts/probe.py --snap     # 打印结构 + 截图（截图落在 $DOLA_ART_DIR）
python scripts/generate.py --list  # 列出当前参数按钮与值
```

跑通一次生成后，把实际命中的选择器/坐标更新上表，标 ✅ 并注明日期。
